/**
 * @file ui_manager_cfg.c
 * @brief Default UI configuration data (if needed)
 *
 * Provides default values that can be changed without touching state machine.
 */

#include "ui_manager_cfg.h"

/* Example: if you want defaults for screen rotation, etc. */
/* Currently no runtime variables needed here, only macros in .h */
